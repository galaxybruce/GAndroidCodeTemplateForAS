
import com.kidswant.common.base.BSBaseView;
import java.util.List;

public interface ${name}Contract {

    interface Presenter {

        // sample method, please delete it
        void performRequest(String activityId);
    }

    interface View extends BSBaseView {

        // sample method, please delete it
        void handleRequestResult(List<String> list);
    }
}
